# Synaploom architecture

The installed runtime is the native Go executable under `cmd/synaploom`. React and TypeScript remain responsible for the browser UI, generated clients, contracts, and authoring utilities. Node is required only for Web development and build tooling.
