# ADR 0002: Go runtime cutover

Synaploom 0.2 promotes the native Go executable under `cmd/synaploom` to the installed product. The final Node 0.1.x package is retained only as a rollback artifact for one release cycle. Existing version-1 courses and learner data remain compatibility boundaries.
